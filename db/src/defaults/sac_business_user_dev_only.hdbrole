{
	"role":{
		"name": "OSR_BUSINESS_SEMANTIC.db.defaults::sac_user_dev_access",
		"schema_privileges": [{
		   "privileges": ["SELECT METADATA",
		    			  "SELECT CDS METADATA",
		    			  "SELECT",
		    			  "INSERT",
		    			  "EXECUTE",
		    			  "DELETE",
		    			  "UPDATE",
		    			  "CREATE TEMPORARY TABLE",
		    			  "DEBUG"
		  ]
		}]
    	}
}